import React from 'react';
import './Header.css'

export default function Header() {
    return (
        <header class="header">
            <a href="">Login</a>
            <a href="">Register</a>
        </header>
    );
}